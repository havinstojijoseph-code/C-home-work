//havins toji department of biotechnology
#include<stdio.h>
void main() 
{
       int x, y, n;
       printf("enter the number of x and y");
       scanf("%d%d%d", &x, &y, &a);
       n=x;
       x=y;
       y=;
       printf("value of x is %d", x);
       printf("value of x is %d", y);
       printf("value of x after swapping is %d", y);
        
}      